defmodule Atrium.Accounts.SamlConfigProvider do
  # Placeholder module for future SAML configuration — see plan 0c task 8.
  # Samly.Provider is a GenServer (not a behaviour), so this module is reserved
  # for any custom configuration helpers needed during full implementation.

  def initialize(_opts), do: :ok

  def refresh, do: :ok
end
